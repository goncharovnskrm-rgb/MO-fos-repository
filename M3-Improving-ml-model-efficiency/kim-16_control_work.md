**Контрольная работа**

**МОДУЛЬ 3. Повышение эффективности ML-моделей**

**Вариант 1**

**1\. Теоретический вопрос:** Модель машинного обучения, отлично работавшая на исторических данных, начала деградировать в продакшене. Разделите дрейф данных на _Covariate shift_ и _Concept shift_. Приведите по одному реалистичному бизнес-примеру для каждого типа дрейфа. Какими базовыми статистическими методами можно детектировать Covariate shift?

**2\. Практическая задача (библиотека autosklearn):** Вам поручено быстро построить бейзлайн для задачи классификации с использованием AutoML. Напишите фрагмент кода, который:

- Инициализирует AutoSklearnClassifier.
- Ограничивает общее время работы алгоритма до 5 минут (300 секунд).
- Запрещает использование линейных моделей (например, логистической регрессии), оставляя только ансамбли на основе деревьев.
- Выводит итоговые веса (weights) моделей, вошедших в финальный ансамбль.

**Вариант 2**

**1\. Теоретический вопрос:** Физико-информированные нейронные сети (PINN) и Neural ODE используют дифференциальные уравнения в архитектуре или функции потерь. _Часть А:_ Опишите, из каких двух основных компонентов (слагаемых) состоит функция потерь (Loss function) в PINN. Какой физический смысл несет каждое из них? _Часть Б:_ Объясните интуитивную связь между остаточным блоком (Residual block) в ResNet и шагом метода Эйлера при численном решении ОДУ.

**2\. Практическая задача (библиотека autosklearn):** В рамках мета-обучения (meta-learning) для ускорения работы AutoML, вы хотите использовать результаты предыдущего запуска или задать приоритет определенным конфигурациям. Напишите фрагмент кода, который:

- Показывает, как получить статистику (leaderboard) по всем оцененным конфигурациям пайплайнов после работы AutoSklearnClassifier.
- Позволяет извлечь лучшую модель (best model) из объекта AutoML для сохранения через pickle или joblib.

**Вариант 3**

**1\. Теоретический вопрос:** AutoML обещает автоматизировать построение ML-пайплайнов. Опишите архитектуру типичной AutoML-системы (например, auto-sklearn): какие пространства моделей и препроцессинга она перебирает, как используется байесовская оптимизация вместо полного перебора? Что такое _мета-обучение_ (meta-learning) в контексте AutoML – как результаты предыдущих запусков на похожих датасетах ускоряют поиск конфигурации для новой задачи? Почему финальный ансамбль в AutoML часто оказывается лучше любой отдельной модели?

**2\. Практическая задача (библиотека autosklearn):** Вам нужно быстро решить задачу регрессии с помощью AutoML, причём данные содержат как числовые, так и категориальные признаки, а также пропуски. Напишите фрагмент кода, который:

- Инициализирует AutoSklearnRegressor с ограничением по времени (например, 360 секунд) и по памяти на одну конфигурацию.
- Задаёт метрику оптимизации r2.
- После обучения выводит топ-5 лучших моделей, вошедших в ансамбль, с их весами.

**Вариант 4**

**1\. Теоретический вопрос:** Модель, предсказывающая отток клиентов, была обучена в 2024 году. К 2026 году её качество упало. Разберите три типа дрейфа: _covariate shift_, _concept shift_ и _prior probability shift_. Для каждого типа приведите пример из области клиентской аналитики. Если вы обнаружили, что распределение признаков изменилось (covariate shift), но зависимость y = f(X) осталась прежней – нужно ли переобучать модель? Какие статистические тесты (например, KS-test, PSI) можно использовать для детектирования дрейфа в продакшене?

**2\. Практическая задача (библиотека autosklearn):** В продакшен-системе вы хотите использовать AutoML с учётом временной структуры данных (нельзя использовать будущее для предсказания прошлого). Напишите фрагмент кода, который:

- Инициализирует AutoSklearnClassifier с временным сплитом (resampling_strategy='cv' с resampling_strategy_arguments для временных рядов, либо holdout на хронологическом срезе).
- Ограничивает пространство поиска только ансамблями деревьев (исключая линейные модели).
- После обучения сохраняет объект AutoML и выводит leaderboard всех оценённых конфигураций через атрибут cv_results_ или leaderboard().
