_ML-2.3 C Способен сделать адекватный выбор типов кросс-валидации в зависимости от объёма данных, баланса классов и наличия временной структуры в датасете чтобы избежать переобучения_

_Способен адекватно оценить качество ML-модели с учетом компромисса между смещением и дисперсией, а также способность модели к обобщению_

_ML-3.1 C Способен осуществлять обоснованный и осознанный выбор классических алгоритмов МО с учителем и выполнять их параметризацию в зависимости от поставленной задачи и имеющихся данных и с пониманием математической природы данных, вычислительных ограничений и требований к интерпретируемости результатов_

_ML-3.2 C Способен комплексно применять методы байесовской классификации и ансамблевые методы МО (бэггинг, бустинг, стэкинг моделей), а также производные от них (случайный лес, градиентный бустинг на деревьях), выдерживая баланс между вероятностной интерпретацией данных и комплексированием алгоритмов_

**Контрольная работа**

**МОДУЛЬ 1. Методы обучения с учителем**

**Вариант 1**

**1\. Теоретический вопрос:** Объясните компромисс между смещением и дисперсией (Bias-Variance Tradeoff). Как изменение параметра max_depth в дереве решений влияет на эти две ошибки? Почему одно дерево решений обычно имеет высокую дисперсию, и как именно Случайный лес (Random Forest) борется с этой проблемой на уровне алгоритма?

**2\. Практическая задача (библиотека scikit-learn):** У вас есть датасет для задачи бинарной классификации с сильным дисбалансом классов (10% положительного класса). Вам необходимо подобрать гиперпараметры модели LogisticRegression (а именно параметр регуляризации C). Напишите фрагмент кода, который:

- Создает стратифицированную кросс-валидацию (StratifiedKFold).
- Настраивает GridSearchCV так, чтобы оптимизировалась метрика roc_auc (а не accuracy).
- Корректно передает данные в fit.

**Вариант 2**

**1\. Теоретический вопрос:** В градиентном бустинге (XGBoost / LightGBM) используются такие параметры, как learning_rate (eta) и n_estimators (num_boost_round). Объясните математическую/интуитивную связь между ними. Что такое Early Stopping, как он реализуется в этих библиотеках и почему он считается методом регуляризации, предотвращающим переобучение?

**2\. Практическая задача (библиотеки lightgbm / xgboost):** Вам нужно обучить LGBMClassifier на датасете, который содержит как числовые, так и категориальные признаки (в формате pandas DataFrame). Напишите фрагмент кода, который:

- Передает категориальные признаки в модель нативно (без One-Hot Encoding).
- Разбивает данные на train/valid.
- Обучает модель с использованием eval_set и early_stopping_rounds.

**Вариант 3**

**1\. Теоретический вопрос:**

В задаче медицинской диагностики редкого заболевания (распространённость 1%) модель показывает Accuracy = 99%. Объясните, почему эта метрика вводит в заблуждение. Сравните поведение ROC-AUC и PR-AUC (Precision-Recall AUC) на таких данных – почему PR-кривая считается более информативной при сильном дисбалансе? Как изменение порога классификации позволяет управлять trade-off между Precision и Recall, и в каком бизнес-сценарии вы бы сознательно выбрали стратегию "лучше ложноположительный, чем ложноотрицательный"?

**2\. Практическая задача (библиотеки scikit-learn, imbalanced-learn):**

У вас есть датасет с числовыми и категориальными признаками, а также с пропусками. Необходимо построить воспроизводимый пайплайн, защищённый от data leakage.

Напишите фрагмент кода, который:

Создаёт ColumnTransformer с разными стратегиями для числовых (SimpleImputer + StandardScaler) и категориальных (OneHotEncoder) признаков.

Оборачивает всё в Pipeline вместе с LogisticRegression.

Применяет GridSearchCV с стратифицированной кросс-валидацией – так, чтобы импутация и масштабирование обучались только на train-фолдах внутри CV.

**Вариант 4**

**1\. Теоретический вопрос:**

Вы построили сложную модель (градиентный бустинг) для скоринга клиентов, и отдел compliance требует объяснить, почему конкретному клиенту был отказано в кредите. Сравните методы SHAP и LIME для решения этой задачи. В чём состоит теоретико-игровая основа SHAP (значения Шепли)? Почему SHAP-значения обладают свойством локальной точности и консистентности, а LIME – нет? Какой из методов вы бы выбрали для (а) объяснения одного предсказания, (б) глобального понимания важности признаков, и почему?

**2\. Практическая задача (библиотеки scikit-learn, shap):**

Вы обучили RandomForestClassifier и хотите проанализировать его с помощью SHAP.

Напишите фрагмент кода, который:

Инициализирует shap.TreeExplainer для обученной модели.

Вычисляет SHAP-значения для тестовой выборки X_test.

Строит summary_plot (можно описать одной командой).

Для конкретного объекта с индексом i извлекает его индивидуальные SHAP-значения и определяет, какой признак внёс наибольший вклад в предсказание положительного класса.
