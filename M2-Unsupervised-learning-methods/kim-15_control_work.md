**Контрольная работа**

**МОДУЛЬ 2. Методы обучения без учителя**

**Вариант 1**

**1\. Теоретический вопрос:** Метод K-Means предполагает, что кластеры имеют сферическую (выпуклую) форму. Представьте, что ваши данные имеют форму "лун" (две пересекающиеся дуги). Объясните, почему K-Means и метрика "коэффициент силуэта" (Silhouette Score) дадут некорректные результаты на таких данных. Какой альтернативный метод кластеризации вы бы применили и почему?

**2\. Практическая задача (библиотека scikit-learn):** Вы применяете метод главных компонент (PCA) для визуализации и сжатия данных. Напишите фрагмент кода, который:

- Инициализирует PCA.
- Обучает его на матрице признаков X.
- С помощью кода (или описания логики) определяет минимальное количество компонент, необходимое для объяснения 95% дисперсии данных.

**Вариант 2**

**1\. Теоретический вопрос:** Сравните методы DBSCAN и t-SNE. Оба метода могут использоваться для визуализации или поиска структуры, но решают разные задачи. В чем фундаментальное отличие их целей? Как параметры eps и min_samples в DBSCAN влияют на выделение точек в категории noise, и как это свойство можно использовать для детектирования аномалий?

**2\. Практическая задача (библиотека scikit-learn):** Вы обучили модель DBSCAN на наборе данных X для поиска аномалий. Напишите фрагмент кода, который:

- Инициализирует и обучает модель.
- Извлекает из обученной модели индексы только тех объектов, которые были помечены как аномалии (шум).
- Кратко поясните, какая метка (label) присваивается шумовым точкам в scikit-learn.

**Вариант 3**

**1\. Теоретический вопрос (на базе вопросов 24, 26):** Вам необходимо провести таксономию видов растений на основе 20 морфометрических признаков. Вы решаете использовать агломеративную иерархическую кластеризацию. Объясните разницу между стратегиями связи single, complete, average и Ward – какие формы кластеров каждая из них предпочитает и как они реагируют на выбросы? Как по дендрограмме определить оптимальное число кластеров, и почему коэффициент силуэта может быть неуместен для оценки качества иерархической таксономии?

**2\. Практическая задача (библиотека scikit-learn):** Вы хотите визуализировать высокоразмерные данные (128 признаков) в 2D с помощью t-SNE, но получаете "шумное облако" без явной структуры. Напишите фрагмент кода, который:

- Инициализирует TSNE с осмысленными параметрами (perplexity, n_iter, random_state).
- Кратко поясните, как параметр perplexity влияет на результат (что он балансирует между учётом локальных и глобальных соседей).
- Объясните, почему перед t-SNE рекомендуется применять PCA, и покажите соответствующую связку в коде.

**Вариант 4**

**1\. Теоретический вопрос:** Объясните феномен "проклятия размерности" (curse of dimensionality) и его влияние на метрики расстояния в задачах кластеризации и KNN. В чём принципиальная разница между _отбором признаков_ (feature selection) и _выделением признаков_ (feature extraction)? Почему PCA, несмотря на свою популярность, может быть неприменим для данных с нелинейной структурой (например, "швейцарский рулет"), и какую альтернативу предлагает Kernel PCA?

**2\. Практическая задача (библиотека scikit-learn):** Вы применили несколько методов кластеризации (K-Means, DBSCAN) к одному датасету и хотите сравнить их качество, не имея истинных меток. Напишите фрагмент кода, который:

- Вычисляет для полученных кластеров три внутренние метрики: Silhouette Score, Davies-Bouldin Index, Calinski-Harabasz Score.
- Кратко поясните, какую из этих метрик следует **максимизировать**, а какую **минимизировать**, и какой физический смысл у каждой.
